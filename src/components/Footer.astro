---
interface Props {
  meta?: string;
}
const { meta = 'bach-nguyen.ipynb · kernel: python 3' } = Astro.props;
---

<footer class="foot">
  <span>{meta}</span>
  <span>built with too much coffee in Oshawa / Toronto</span>
</footer>
